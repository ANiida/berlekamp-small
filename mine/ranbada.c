#include "global.h"
#include "struct.h"
#include "8192.h"

extern vec renritu(MTX a);
extern oterm vLT(vec f);
extern vec vpp(vec f, vec mod);
extern vec vadd(vec a, vec b);
extern vec vgcd(vec xx, vec yy);
extern void GF_mul(unsigned short *out, unsigned short *in0, unsigned short *in1);
extern int ben_or(vec f);
extern oterm vLT(vec f);
extern int deg(vec a);
extern unsigned short oinv(unsigned short a);
extern void GF_mul(unsigned short *out, unsigned short *in0, unsigned short *in1);
extern int ben_or(vec f);
extern int mlt(int x, int y);
extern int mltn(int n, int x);
extern vec renritu(MTX a);
extern vec o2v(OP f);
extern OP v2o(vec a);
extern vec vmul(vec a, vec b);
extern vec vadd(vec a, vec b);
extern vec kof2(unsigned short c, vec f);

